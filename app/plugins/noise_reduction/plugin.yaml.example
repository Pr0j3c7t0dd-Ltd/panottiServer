name: "noise_reduction"
version: "1.0.0"
enabled: true
dependencies: []
config:
  # Audio processing settings
  highpass_cutoff: 60      # Cutoff frequency for highpass filter (Hz)
  highpass_order: 3        # Filter order (higher = sharper cutoff)
  
  # System settings
  output_directory: "data/cleaned_audio"  # Directory for processed files
  max_concurrent_tasks: 4   # Maximum number of concurrent processing tasks